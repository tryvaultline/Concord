// Concord Xcode Build Configuration - Centralized Immutable Bundle Identifiers
// DO NOT MODIFY OR ALTER BUNDLE IDENTIFIERS IN FUTURE COMMITS

CONCORD_PRODUCT_NAME = Concord
CONCORD_BUNDLE_IDENTIFIER_BASE = app.concord.ios

CONCORD_BUNDLE_IDENTIFIER_APP = app.concord.ios
CONCORD_BUNDLE_IDENTIFIER_SHARE = app.concord.ios.share
CONCORD_BUNDLE_IDENTIFIER_NSE = app.concord.ios.notification-service
CONCORD_BUNDLE_IDENTIFIER_INTENTS = app.concord.ios.intents

CONCORD_APP_GROUP = group.app.concord.ios
CONCORD_KEYCHAIN_GROUP = $(AppIdentifierPrefix)app.concord.ios
